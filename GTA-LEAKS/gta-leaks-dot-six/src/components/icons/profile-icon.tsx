import { IonIcon } from "@ionic/react";
import { logoIonic, personCircleOutline } from "ionicons/icons";

const ProfileIcon = () => {
    return (
        <>
            <IonIcon icon={personCircleOutline} size="large"></IonIcon>
        </>
    );
};

export default ProfileIcon;
